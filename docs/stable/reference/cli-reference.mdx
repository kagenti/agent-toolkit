---
title: "CLI Reference"
description: "Complete reference for the Kagenti ADK command-line interface"
---

The Kagenti ADK command-line interface (CLI) provides tools to manage agents, configure models, and control the platform directly from your terminal.

{/*

⚠️⚠️⚠️ WARNING ⚠️⚠️⚠️

DO NOT EDIT THIS FILE BY HAND, IT IS AUTOMATICALLY GENERATED BY `mise adk-cli:docs`

*/}
{/* usage-generator */}

**Usage**:

```console
$ kagenti-cli [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.
* `--install-completion`: Install completion for the current shell.
* `--show-completion`: Show completion for the current shell, to copy it or customize the installation.

**Commands**:

* `version`: Print version of the Agent Stack CLI.
* `ui`: Launch the graphical interface.
* `model`: Manage model providers.
* `agent`: Manage agents.
* `connector`: Manage connectors to external services.
* `platform`: Manage Agent Stack platform.
* `build`: Build an agent image locally and push to platform registry.
* `server`: Manage Agent Stack servers and...
* `self`: Manage Agent Stack installation.
* `user`: Manage users.
* `add`: Add a docker image or GitHub repository.
* `update`: Upgrade agent to a newer docker image or...
* `remove | uninstall | rm | delete`: Remove agent.
* `logs`: Stream agent provider logs.
* `run`: Run an agent.
* `list`: List agents.
* `info`: Show agent details.
* `env`
* `feedback`: Manage user feedback for your agents

## `kagenti-cli version`

Print version of the Agent Stack CLI.

**Usage**:

```console
$ kagenti-cli version [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

## `kagenti-cli ui`

Launch the graphical interface.

**Usage**:

```console
$ kagenti-cli ui [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

## `kagenti-cli model`

Manage model providers. [Admin only]

**Usage**:

```console
$ kagenti-cli model [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `list`: List all available models.
* `setup`: Interactive setup for LLM and embedding...
* `change | select | default`: Change the default model [Admin only]
* `add`: Add a new model provider.
* `remove | rm | delete`
* `provider`

### `kagenti-cli model list`

List all available models.

**Usage**:

```console
$ kagenti-cli model list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli model setup`

Interactive setup for LLM and embedding provider environment variables [Admin only]

**Usage**:

```console
$ kagenti-cli model setup [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli model change | select | default`

Change the default model [Admin only]

**Usage**:

```console
$ kagenti-cli model change | select | default [OPTIONS] [CAPABILITY]:[llm|embedding] [MODEL_ID]
```

**Arguments**:

* `[CAPABILITY]:[llm|embedding]`: Which default model to change (llm/embedding)
* `[MODEL_ID]`: Model ID to be used as default

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli model add`

Add a new model provider. [Admin only]

**Usage**:

```console
$ kagenti-cli model add [OPTIONS] [CAPABILITY]:[llm|embedding]
```

**Arguments**:

* `[CAPABILITY]:[llm|embedding]`: Which default model to change (llm/embedding)

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli model remove | rm | delete`

**Usage**:

```console
$ kagenti-cli model remove | rm | delete [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Provider type or part of the provider base url

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli model provider`

**Usage**:

```console
$ kagenti-cli model provider [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `list`: List all available model providers.
* `add`: Add a new model provider [Admin only]
* `remove | rm | delete`: Remove a model provider [Admin only]

#### `kagenti-cli model provider list`

List all available model providers.

**Usage**:

```console
$ kagenti-cli model provider list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

#### `kagenti-cli model provider add`

Add a new model provider [Admin only]

**Usage**:

```console
$ kagenti-cli model provider add [OPTIONS] [CAPABILITY]:[llm|embedding]
```

**Arguments**:

* `[CAPABILITY]:[llm|embedding]`: Which default model to change (llm/embedding)

**Options**:

* `--help`: Show this message and exit.

#### `kagenti-cli model provider remove | rm | delete`

Remove a model provider [Admin only]

**Usage**:

```console
$ kagenti-cli model provider remove | rm | delete [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Provider type or part of the provider base url

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

## `kagenti-cli agent`

Manage agents. Some commands are [Admin only].

**Usage**:

```console
$ kagenti-cli agent [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `add`: Add a docker image or GitHub repository.
* `update`: Upgrade agent to a newer docker image or...
* `remove | uninstall | rm | delete`: Remove agent.
* `logs`: Stream agent provider logs.
* `run`: Run an agent.
* `list`: List agents.
* `info`: Show agent details.
* `env`
* `feedback`: Manage user feedback for your agents

### `kagenti-cli agent add`

Add a docker image or GitHub repository. [Admin only]

This command supports a variety of GitHub URL formats for deploying agents:

- **Basic URL**: `https://github.com/myorg/myrepo`
- **Git Protocol URL**: `git+https://github.com/myorg/myrepo`
- **URL with .git suffix**: `https://github.com/myorg/myrepo.git`
- **URL with Version Tag**: `https://github.com/myorg/myrepo@v1.0.0`
- **URL with Branch Name**: `https://github.com/myorg/myrepo@my-branch`
- **URL with Subfolder Path**: `https://github.com/myorg/myrepo#path=/path/to/agent`
- **Combined Formats**: `https://github.com/myorg/myrepo.git@v1.0.0#path=/path/to/agent`
- **Enterprise GitHub**: `https://github.mycompany.com/myorg/myrepo`
- **With a custom Dockerfile location**: `kagenti-cli add --dockerfile /my-agent/path/to/Dockerfile &quot;https://github.com/my-org/my-awesome-agents@main#path=/my-agent&quot;`

**Usage**:

```console
$ kagenti-cli agent add [OPTIONS] [LOCATION]
```

**Arguments**:

* `[LOCATION]`: Agent location (public docker image or github url)

**Options**:

* `--dockerfile TEXT`: Use custom dockerfile path
* `-v, --verbose`: Show verbose output
* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli agent update`

Upgrade agent to a newer docker image or build from GitHub repository. [Admin only]

**Usage**:

```console
$ kagenti-cli agent update [OPTIONS] [SEARCH_PATH] [LOCATION]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location of agent to replace
* `[LOCATION]`: Agent location (public docker image or github url)

**Options**:

* `--dockerfile TEXT`: Use custom dockerfile path
* `-v, --verbose`: Show verbose output
* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli agent remove | uninstall | rm | delete`

Remove agent. [Admin only]

**Usage**:

```console
$ kagenti-cli agent remove | uninstall | rm | delete [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `-a, --all`: Remove all agents without selection.
* `--help`: Show this message and exit.

### `kagenti-cli agent logs`

Stream agent provider logs. [Admin only]

**Usage**:

```console
$ kagenti-cli agent logs [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli agent run`

Run an agent.

**Usage**:

```console
$ kagenti-cli agent run [OPTIONS] [SEARCH_PATH] [INPUT]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location
* `[INPUT]`: Agent input as text or JSON

**Options**:

* `--dump-files PATH`: Folder path to save any files returned by the agent
* `--help`: Show this message and exit.

### `kagenti-cli agent list`

List agents.

**Usage**:

```console
$ kagenti-cli agent list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli agent info`

Show agent details.

**Usage**:

```console
$ kagenti-cli agent info [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli agent env`

**Usage**:

```console
$ kagenti-cli agent env [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `add`: Store environment variables.
* `list`: List stored environment variables.
* `remove`: Remove environment variable(s).

#### `kagenti-cli agent env add`

Store environment variables. [Admin only]

**Usage**:

```console
$ kagenti-cli agent env add [OPTIONS] SEARCH_PATH ENV...
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]
* `ENV...`: Environment variables to pass to agent  [required]

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

#### `kagenti-cli agent env list`

List stored environment variables. [Admin only]

**Usage**:

```console
$ kagenti-cli agent env list [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

#### `kagenti-cli agent env remove`

Remove environment variable(s). [Admin only]

**Usage**:

```console
$ kagenti-cli agent env remove [OPTIONS] SEARCH_PATH ENV...
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]
* `ENV...`: Environment variable(s) to remove  [required]

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli agent feedback`

Manage user feedback for your agents

**Usage**:

```console
$ kagenti-cli agent feedback [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `list`: List your agent feedback.

#### `kagenti-cli agent feedback list`

List your agent feedback. [Admin only]

**Usage**:

```console
$ kagenti-cli agent feedback list [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location

**Options**:

* `--limit INTEGER`: Number of results per page   [default: 50]
* `--after TEXT`: Cursor for pagination
* `--help`: Show this message and exit.

## `kagenti-cli connector`

Manage connectors to external services.

**Usage**:

```console
$ kagenti-cli connector [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `create`: Create a connector to an external service.
* `remove | rm | delete`: Remove connectors.
* `list`: List all connectors.
* `list-presets`: List connector presets.
* `get`: Get connector details.
* `connect`: Connect a connector (e.g., start OAuth flow).
* `disconnect`: Disconnect one or more connectors.

### `kagenti-cli connector create`

Create a connector to an external service.

**Usage**:

```console
$ kagenti-cli connector create [OPTIONS] URL
```

**Arguments**:

* `URL`: Agent location (public docker image or github url)  [required]

**Options**:

* `--client-id TEXT`: Client ID for authentication, acquired from env if not supplied
* `--client-secret TEXT`: Client secret for authentication, acquired from env if not supplied
* `--metadata TEXT`: Metadata as JSON string
* `--match-preset`: Use preset configuration for given url if it exists  [default: True]
* `--help`: Show this message and exit.

### `kagenti-cli connector remove | rm | delete`

Remove connectors.

**Usage**:

```console
$ kagenti-cli connector remove | rm | delete [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID or connector url, supports partial matching

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `-a, --all`: Remove all connectors without selection.
* `--help`: Show this message and exit.

### `kagenti-cli connector list`

List all connectors.

**Usage**:

```console
$ kagenti-cli connector list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli connector list-presets`

List connector presets.

**Usage**:

```console
$ kagenti-cli connector list-presets [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli connector get`

Get connector details.

**Usage**:

```console
$ kagenti-cli connector get [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID or connector url, supports partial matching  [required]

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli connector connect`

Connect a connector (e.g., start OAuth flow).

**Usage**:

```console
$ kagenti-cli connector connect [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID or connector url, supports partial matching  [required]

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli connector disconnect`

Disconnect one or more connectors.

**Usage**:

```console
$ kagenti-cli connector disconnect [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID or connector url, supports partial matching

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `-a, --all`: Deisconnect all connectors without selection.
* `--help`: Show this message and exit.

## `kagenti-cli platform`

Manage Agent Stack platform. [Local only]

**Usage**:

```console
$ kagenti-cli platform [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `start`: Start Agent Stack platform.
* `stop`: Stop Agent Stack platform.
* `delete`: Delete Agent Stack platform.
* `import`: Import a local docker image into the Agent...
* `exec`: For debugging -- execute a command inside...

### `kagenti-cli platform start`

Start Agent Stack platform. [Local only]

**Usage**:

```console
$ kagenti-cli platform start [OPTIONS]
```

**Options**:

* `--set TEXT`: Set Helm chart values using &lt;key&gt;=&lt;value&gt; syntax  [default: &lt;class &#x27;list&#x27;&gt;]
* `--image-pull-mode <guest|host|hybrid>`:
    * `guest`: Pull all images inside VM
    * `host`: Pull unavailable images on host, then import all
    * `hybrid`: Import available images from host, pull the rest in VM
    * `skip`: Skip explicit pull step (Kubernetes will attempt to pull missing images)
* `-f PATH`: Set Helm chart values using yaml values file
* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli platform stop`

Stop Agent Stack platform. [Local only]

**Usage**:

```console
$ kagenti-cli platform stop [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli platform delete`

Delete Agent Stack platform. [Local only]

**Usage**:

```console
$ kagenti-cli platform delete [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli platform import`

Import a local docker image into the Agent Stack platform. [Local only]

**Usage**:

```console
$ kagenti-cli platform import [OPTIONS] TAG
```

**Arguments**:

* `TAG`: Docker image tag to import  [required]

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli platform exec`

For debugging -- execute a command inside the Agent Stack platform VM. [Local only]

**Usage**:

```console
$ kagenti-cli platform exec [OPTIONS] [COMMAND]...
```

**Arguments**:

* `[COMMAND]...`

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

## `kagenti-cli build`

Build an agent image locally and push it to the platform registry. [Local only]

**Usage**:

```console
$ kagenti-cli build [OPTIONS] [CONTEXT]
```

**Arguments**:

* `[CONTEXT]`: Docker build context (path or URL)  [default: .]

**Options**:

* `-f, --dockerfile TEXT`: Dockerfile path
* `-t, --tag TEXT`: Image tag (default: auto-generated)
* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

## `kagenti-cli server`

Manage Agent Stack servers and authentication.

**Usage**:

```console
$ kagenti-cli server [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `login | change | select | default | switch`: Login to a server or switch between logged...
* `logout | remove | rm | delete`
* `show`
* `list`

### `kagenti-cli server login | change | select | default | switch`

Login to a server or switch between logged in servers.

**Usage**:

```console
$ kagenti-cli server login | change | select | default | switch [OPTIONS] [SERVER]
```

**Arguments**:

* `[SERVER]`

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli server logout | remove | rm | delete`

**Usage**:

```console
$ kagenti-cli server logout | remove | rm | delete [OPTIONS]
```

**Options**:

* `--all / --no-all`: [default: no-all]
* `--help`: Show this message and exit.

### `kagenti-cli server show`

**Usage**:

```console
$ kagenti-cli server show [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli server list`

**Usage**:

```console
$ kagenti-cli server list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

## `kagenti-cli self`

Manage Agent Stack installation.

**Usage**:

```console
$ kagenti-cli self [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `version`: Print version of the Agent Stack CLI.
* `install`: Install Agent Stack platform pre-requisites.
* `upgrade`: Upgrade Agent Stack CLI and Platform to...
* `uninstall`: Uninstall Agent Stack CLI and Platform.

### `kagenti-cli self version`

Print version of the Agent Stack CLI.

**Usage**:

```console
$ kagenti-cli self version [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli self install`

Install Agent Stack platform pre-requisites.

**Usage**:

```console
$ kagenti-cli self install [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli self upgrade`

Upgrade Agent Stack CLI and Platform to the latest version.

**Usage**:

```console
$ kagenti-cli self upgrade [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

### `kagenti-cli self uninstall`

Uninstall Agent Stack CLI and Platform.

**Usage**:

```console
$ kagenti-cli self uninstall [OPTIONS]
```

**Options**:

* `-v, --verbose`: Show verbose output
* `--help`: Show this message and exit.

## `kagenti-cli user`

Manage users. [Admin only]

**Usage**:

```console
$ kagenti-cli user [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `list`: List platform users [Admin only]
* `set-role`: Change user role [Admin only]

### `kagenti-cli user list`

List platform users [Admin only]

**Usage**:

```console
$ kagenti-cli user list [OPTIONS]
```

**Options**:

* `--email TEXT`: Filter by email (case-insensitive partial match)
* `--limit INTEGER`: Results per page (1-100)  [default: 40]
* `--after TEXT`: Pagination cursor (page_token)
* `--help`: Show this message and exit.

### `kagenti-cli user set-role`

Change user role [Admin only]

**Usage**:

```console
$ kagenti-cli user set-role [OPTIONS] USER_ID ROLE:{admin|developer|user}
```

**Arguments**:

* `USER_ID`: User UUID  [required]
* `ROLE:{admin|developer|user}`: Target role (admin, developer, user)  [required]

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

## `kagenti-cli add`

Add a docker image or GitHub repository. [Admin only]

This command supports a variety of GitHub URL formats for deploying agents:

- **Basic URL**: `https://github.com/myorg/myrepo`
- **Git Protocol URL**: `git+https://github.com/myorg/myrepo`
- **URL with .git suffix**: `https://github.com/myorg/myrepo.git`
- **URL with Version Tag**: `https://github.com/myorg/myrepo@v1.0.0`
- **URL with Branch Name**: `https://github.com/myorg/myrepo@my-branch`
- **URL with Subfolder Path**: `https://github.com/myorg/myrepo#path=/path/to/agent`
- **Combined Formats**: `https://github.com/myorg/myrepo.git@v1.0.0#path=/path/to/agent`
- **Enterprise GitHub**: `https://github.mycompany.com/myorg/myrepo`
- **With a custom Dockerfile location**: `kagenti-cli add --dockerfile /my-agent/path/to/Dockerfile &quot;https://github.com/my-org/my-awesome-agents@main#path=/my-agent&quot;`

**Usage**:

```console
$ kagenti-cli add [OPTIONS] [LOCATION]
```

**Arguments**:

* `[LOCATION]`: Agent location (public docker image or github url)

**Options**:

* `--dockerfile TEXT`: Use custom dockerfile path
* `-v, --verbose`: Show verbose output
* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

## `kagenti-cli update`

Upgrade agent to a newer docker image or build from GitHub repository. [Admin only]

**Usage**:

```console
$ kagenti-cli update [OPTIONS] [SEARCH_PATH] [LOCATION]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location of agent to replace
* `[LOCATION]`: Agent location (public docker image or github url)

**Options**:

* `--dockerfile TEXT`: Use custom dockerfile path
* `-v, --verbose`: Show verbose output
* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

## `kagenti-cli remove | uninstall | rm | delete`

Remove agent. [Admin only]

**Usage**:

```console
$ kagenti-cli remove | uninstall | rm | delete [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `-a, --all`: Remove all agents without selection.
* `--help`: Show this message and exit.

## `kagenti-cli logs`

Stream agent provider logs. [Admin only]

**Usage**:

```console
$ kagenti-cli logs [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

## `kagenti-cli run`

Run an agent.

**Usage**:

```console
$ kagenti-cli run [OPTIONS] [SEARCH_PATH] [INPUT]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location
* `[INPUT]`: Agent input as text or JSON

**Options**:

* `--dump-files PATH`: Folder path to save any files returned by the agent
* `--help`: Show this message and exit.

## `kagenti-cli list`

List agents.

**Usage**:

```console
$ kagenti-cli list [OPTIONS]
```

**Options**:

* `--help`: Show this message and exit.

## `kagenti-cli info`

Show agent details.

**Usage**:

```console
$ kagenti-cli info [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

## `kagenti-cli env`

**Usage**:

```console
$ kagenti-cli env [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `add`: Store environment variables.
* `list`: List stored environment variables.
* `remove`: Remove environment variable(s).

### `kagenti-cli env add`

Store environment variables. [Admin only]

**Usage**:

```console
$ kagenti-cli env add [OPTIONS] SEARCH_PATH ENV...
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]
* `ENV...`: Environment variables to pass to agent  [required]

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

### `kagenti-cli env list`

List stored environment variables. [Admin only]

**Usage**:

```console
$ kagenti-cli env list [OPTIONS] SEARCH_PATH
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]

**Options**:

* `--help`: Show this message and exit.

### `kagenti-cli env remove`

Remove environment variable(s). [Admin only]

**Usage**:

```console
$ kagenti-cli env remove [OPTIONS] SEARCH_PATH ENV...
```

**Arguments**:

* `SEARCH_PATH`: Short ID, agent name or part of the provider location  [required]
* `ENV...`: Environment variable(s) to remove  [required]

**Options**:

* `-y, --yes`: Skip confirmation prompts.
* `--help`: Show this message and exit.

## `kagenti-cli feedback`

Manage user feedback for your agents

**Usage**:

```console
$ kagenti-cli feedback [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `--help`: Show this message and exit.

**Commands**:

* `list`: List your agent feedback.

### `kagenti-cli feedback list`

List your agent feedback. [Admin only]

**Usage**:

```console
$ kagenti-cli feedback list [OPTIONS] [SEARCH_PATH]
```

**Arguments**:

* `[SEARCH_PATH]`: Short ID, agent name or part of the provider location

**Options**:

* `--limit INTEGER`: Number of results per page   [default: 50]
* `--after TEXT`: Cursor for pagination
* `--help`: Show this message and exit.
